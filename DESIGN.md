# CoffeeScale 设计哲学 — 「暖仪式 · Warm Ritual」

> 手冲咖啡智能冲煮称 · 视觉设计系统 v1（2026-09-11）
> 本文档是全应用视觉语言的**单一事实来源**：任何界面迭代（色彩、字阶、组件、交互）先对齐本文，再动手。

---

## 一、美学运动宣言

**暖仪式（Warm Ritual）**——手冲咖啡是一场缓慢的、可被测量的仪式。我们不把"专业"做成冰冷仪表盘，也不把"温暖"做成模糊卡通风；我们做的是**深色木质吧台上一杯正在呼吸的咖啡**：琥珀色的灯光落在浅奶油色的字上，蒸汽缓慢升起，杯渍印在台面上，而精密的刻度与数字安静地陪伴着仪式的每一段注水。

三个关键词：

1. **温度（Warmth）**——一切表面都带咖啡棕的暖底，从不出现纯黑纯灰。
2. **仪式（Ritual）**——信息层级如注水分段：一次只强调一件事，主按钮如注水般唯一且醒目。
3. **精度（Precision）**——数字是主角：整数、克、秒、℃；装饰永远退让于数据。

## 二、色彩系统（Palette）

### 表面与文字
| 语义 | 名称 | 色值 | 用途 |
|------|------|------|------|
| 背景 | Cocoa | `#241710` | 根背景，暖深棕 |
| 卡片 | Espresso | `#33231A` | 卡身（玻璃态时 alpha 214） |
| 面板 | RoastDeep | `#1E130D` | 内嵌面板/输入框底 |
| 主文字 | Cream | `#F3E9DD` | 标题与数值 |
| 次文字 | Foam | `#C9B7A6` | 描述、副标题 |
| 强调 | Caramel | `#E8C79A` | 高亮、口号、图标 |
| 主按钮 | Cinnamon | `#C8893A` | 渐变基色 `#D9974A→#B3742E` |

### 模块色（一人一色，贯穿卡片/瓷砖/标语）
| 模块 | 名称 | 色值 |
|------|------|------|
| 新手模式 | SageGreen | `#7FB069` |
| 专业模式 | SteelBlue | `#4F8FC4` |
| 简易计算器 | SiennaOrange | `#D98246` |
| 手冲大师 | RoyalGold | `#D4A24C` |

**规则**：语义色 > 装饰色；每屏同时出现的彩色 ≤ 2 种；危险红 `#C0392B` 只用于破坏性动作，且**永远保持纯色**（渐变只属于主操作按钮）。

## 三、字阶（Type Scale）

| 层级 | 字号 | 字重 | 用途 |
|------|------|------|------|
| Display | 26 / 19* | Bold | Hero 软件名 |
| Title | 19 / 16* | Bold | 页面标题（页头卡内） |
| Section | 14 | Bold | 区块小标题（①②③ 步骤） |
| Body | 13 | Normal | 正文、横幅文案 |
| Data | 17 | Bold + Caramel | 数值卡数字（视觉主角） |
| Caption | 10.5–11 | Normal | 标签、署名 |

*斜杠前后 = 宽屏 / 紧凑（`_scale` 0.92–1.05 由 FormFactor 决定）。

## 四、组件语言

- **模块卡**：左 5dp 模块色条（贯通全高）+ 图标渐变瓷砖（44dp、45° 对角、模块色→深咖啡）+ 玻璃卡身（Espresso α214 + 1px Cream α26 描边）+ 悬停泛模块色光。
- **按钮**：主操作 = Cinnamon 垂直渐变 + 1px 深描边 + 圆角 10；次操作 = Leather `#3A2A1C` 纯色；危险 = 纯红。全部 MinHeight ≥ 44，按压 Opacity 0.85。
- **选中态**：一律用**模块色**填充 + 1px 深描边（如新手模式滤杯/烘焙度卡 = SageGreen）；分页标签选中 = 主按钮同款渐变（"渐变 = 可点的主操作"认知统一）。
- **提示横幅**：暖底 `#2E2418` + 左 3dp 焦糖竖条 + 13pt 文案。
- **区块标题**：左 3dp 焦糖竖条 + Section 字阶（14pt Bold）——与横幅同语法，左缘对齐形成阅读导轨。
- **数值瓷片**：暖底 `#2A1E13` + 1px 焦糖 α36 描边，数值 17pt 焦糖金。
- **背景装饰**：角落暖光椭圆（径向渐变出血）+ 咖啡渍圆环 + 蒸汽曲线，全部 `IsHitTestVisible=false`、不随内容滚动；子页面仅保留暖光（轻量版）。

## 五、交互与触摸

- **命中目标**：≥ 44×44（HIG/Material），行内小操作 ≥ 60 宽。
- **反馈**：按压 = 整体淡化 15%（立即可感知、无延迟）；悬停 = 背景提亮（桌面）；滑动翻页 = 手机竖屏左右手势（阈值 60px，纵向滚动让路）。
- **节奏**：Hero ☕ 呼吸（2.4s ±5%）是全应用唯一常驻动画——仪式感来自克制。

## 六、响应式（FormFactor）

| 形态 | 判定 | 字号 `_scale` | 布局 |
|------|------|--------------|------|
| Phone | 移动端短边≤420 且长边≤920，竖屏 | 0.92 | 左右分页 + 滑动手势 |
| PhoneLandscape | 同上，横屏 | 0.92 | 双栏，矮屏压缩 Hero |
| Tablet | 其余移动端 / 桌面 640–1024 | 0.95 | 双栏或 2×2 卡片 |
| Desktop | 1024–1600 | 1.00 | 双栏 + 2×2 卡片 |
| Wide | ≥1600 | 1.05 | 同 Desktop，字号略增 |

## 七、工艺标准

每一屏都必须经得起"放大细看"：间距成 rhythm（4/8/12/16）、圆角成体系（8/10/14/16）、透明度成档（20/26/36/46/85%）。宁少一件装饰，不错一处对齐。
